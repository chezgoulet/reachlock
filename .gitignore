# Rust (Cargo.lock is TRACKED: this workspace ships binaries)
target/

# Godot (v1, archived)
.godot/
import/
*.import
.android/
.ios/
.mono/
archive/v1/godot/.godot/
archive/v1/godot/build/
# export_presets.cfg is TRACKED on purpose: ours carries no secrets, and
# v1 CI exported the player artifacts from it.

# Go (v1, archived)
vendor/
archive/v1/server/bin/
archive/v1/server/vendor/

# Pre-existing local draft, kept untracked on purpose (see v1 commit 36134b6)
archive/v1/docs/SPRINT-02-the-living-slice.md

# Mods (built packages)
*.reachmod

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Local agent worktrees / session state
.claude/

# Runtime saves (S16B: the dev save was churning in every playtest commit)
save/
